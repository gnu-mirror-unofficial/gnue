<h1>Document Store</h1>

<p>GNU Enterprise Document Store manages the storage, versioning, tracking and 
indexing of documents and document-like datasets. It would be 
implemented as a series of server objects communicating via an 
RPC mechanism such as CORBA which pass XML tagged data.</p>

<h3>Status</h3>

<$ status ( "docstore" )> 